import type { Auth } from "firebase/auth";
import type { FirebaseApp } from "firebase/app";

export declare const auth: Auth;
export declare const analyticsPromise: Promise<unknown | null>;
declare const app: FirebaseApp;
export default app;
